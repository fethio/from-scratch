#+STARTUP: overview
#+CREATOR: fethi okyar
#+LANGUAGE: en
#+OPTIONS: num:nil toc:nil
#+ATTR_HTML: :style margin-left: auto; margin-right: auto;
#+TITLE: emacs configuration from scratch

* startup
something that uses incf causes the requirement for cl-lib 
#+BEGIN_SRC emacs-lisp
  (require 'cl-lib)
#+END_SRC
ensure compat is installed and loaded before transient and magit load
#+BEGIN_SRC emacs-lisp
(use-package compat
  :ensure t)
#+END_SRC
** Font Configuration
#+BEGIN_SRC emacs-lisp
  ;; Global default font size (160 = 16pt, adjust as needed)
  (set-face-attribute 'default nil :height 124)

  ;; Minibuffer prompt text size
  (set-face-attribute 'minibuffer-prompt nil :height 124)

  ;; Minibuffer input text size (relative to default; 1.0 = same as default)
  (add-hook 'minibuffer-setup-hook
            (lambda ()
              (setq-local face-remapping-alist '((default (:height 1.0) default)))))

  ;; Optional: a bit more line spacing for readability
  ;; (setq-default line-spacing )
#+END_SRC

** mac only settings
#+begin_src emacs-lisp
  (when (eq system-type 'darwin)
    (set-face-attribute 'default nil :height 150)
    (set-face-attribute 'minibuffer-prompt nil :height 150)
    (setq mac-option-modifier 'meta)
    (setq mac-right-option-modifier 'none)
    (setenv "PKG_CONFIG_PATH"
            (concat "/usr/local/opt/curl/lib/pkgconfig:"
                    (or (getenv "PKG_CONFIG_PATH") ""))))
#+end_src

** dashboard
enable emacs dashboard
#+BEGIN_SRC emacs-lisp
  (use-package dashboard
    :ensure t
    :config
    (dashboard-setup-startup-hook)
    (setq dashboard-items '((recents . 15)))
    (setq dashboard-banner-logo-title "Hello UncleDave!"))
#+END_SRC

** Enable emacs server
:PROPERTIES:
Is the server started by the xinit process? do we really need the line below? Or should it be done here?
#+begin_src emacs-lisp
  (load "server")
  (unless (server-running-p) (server-start))
#+end_src

** edit as sudo
#+BEGIN_SRC emacs-lisp
  (use-package sudo-edit
    :ensure t
    :bind
    ("s-e" . sudo-edit))
#+END_SRC

* modeline

* buffers and windows
Another big thing is, buffers. If you use emacs, you use buffers, everyone loves them.
Having many buffers is useful, but can be tedious to work with, let us see how we can improve it.
#+BEGIN_SRC emacs-lisp
  (desktop-save-mode 1)
#+END_SRC

** Always murder current buffer
Doing =C-x k= should kill the current buffer at all times, we have =ibuffer= for more sophisticated thing.
#+BEGIN_SRC emacs-lisp
  (defun kill-current-buffer ()
    "Kills the current buffer."
    (interactive)
    (kill-buffer (current-buffer)))
  (global-set-key (kbd "C-x k") 'kill-current-buffer)
#+END_SRC

** Kill buffers without asking for confirmation
#+BEGIN_SRC emacs-lisp
  (setq kill-buffer-query-functions (delq 'process-kill-buffer-query-function kill-buffer-query-functions))
#+END_SRC

** Turn switch-to-buffer into ibuffer
#+BEGIN_SRC emacs-lisp
  (global-set-key (kbd "C-x b") 'ibuffer)
#+END_SRC

** expert-mode
#+BEGIN_SRC emacs-lisp
  (setq ibuffer-expert t)
#+END_SRC

** close-all-buffers
This can be invoked using =C-M-s-k=.
#+BEGIN_SRC emacs-lisp
  (defun close-all-buffers ()
    "Kill all buffers without regard for their origin."
    (interactive)
    (mapc 'kill-buffer (buffer-list)))
  (global-set-key (kbd "C-M-s-k") 'close-all-buffers)
#+END_SRC

** save buffers for next session
#+BEGIN_SRC emacs-lisp
  (setq save-some-buffers-kill-terminal 'kill)
#+END_SRC

* interface settings
** startup settings
#+BEGIN_SRC emacs-lisp
  (menu-bar-mode -1)
  (tool-bar-mode -1)
  (scroll-bar-mode -1)
  (setq inhibit-startup-message t)
#+END_SRC

** cursor motion, etc
*** highlighting, beacon
#+BEGIN_SRC emacs-lisp
  (when window-system (global-hl-line-mode t))

  (use-package beacon
    :ensure t
    :init
    (beacon-mode 1))
#+END_SRC

*** backward/forward word in camelCase
#+BEGIN_SRC emacs-lisp
  (global-subword-mode 1)
#+END_SRC

*** electric parenthesis
#+BEGIN_SRC emacs-lisp
  (setq electric-pairs '(
                         (?\( . ?\))
                         (?\[ . ?\])
                         ))
  (electric-pair-mode t)
#+END_SRC

** terminal stuff
ansi term uses zsh
#+BEGIN_SRC emacs-lisp
  (defvar my-term-shell "/bin/zsh")
  (defadvice ansi-term (before force-bash)
    (interactive (list my-term-shell)))
  (ad-activate 'ansi-term)

  (global-set-key (kbd "<s-return>") 'ansi-term)
#+END_SRC

** show lines and columns
#+BEGIN_SRC emacs-lisp
  (line-number-mode 1)
  (column-number-mode 1)
#+END_SRC

** Expand region
#+BEGIN_SRC emacs-lisp
  (use-package expand-region
    :ensure t
    :bind ("C-q" . er/expand-region))
#+END_SRC

* helpers
** ivy + counsel — unified completion framework
ivy is the completion engine; counsel provides ivy-enhanced versions of
standard Emacs commands; swiper provides ivy-based buffer search.
ido and smex are replaced by this trio.
#+BEGIN_SRC emacs-lisp
    (use-package ivy
      :ensure t
      :config
      (ivy-mode 1)
      (setq ivy-use-virtual-buffers t)
      (setq ivy-count-format "(%d/%d) "))

    (use-package counsel
    :ensure t
    :after ivy
    :demand t
    :config (counsel-mode 1)
    :bind
    ("M-x"     . counsel-M-x)
    ("C-x C-f" . counsel-find-file)
    ("C-c g"   . counsel-git)
    ("C-c j"   . counsel-git-grep))
  
      (use-package swiper
      :ensure t
      :after ivy
      :bind ("C-s" . swiper))
#+END_SRC

#+RESULTS:
: swiper

** smex — frecency history backend for counsel-M-x
smex is kept as a history backend only; counsel-M-x uses it automatically
when installed. The direct M-x binding is now handled by counsel above.
#+BEGIN_SRC emacs-lisp
  (use-package smex
    :ensure t)
#+END_SRC

** which-key
#+BEGIN_SRC emacs-lisp
  (use-package which-key
    :ensure t
    :init
    (which-key-mode 1))
#+END_SRC

** avy
avy operates on visible buffer characters — completely orthogonal to
the minibuffer completion stack, no conflicts.
#+BEGIN_SRC emacs-lisp
  (use-package avy
    :ensure t
    :bind
    ("M-s" . avy-goto-char))
#+END_SRC

** auto-completion (company)
irony and company-irony are replaced by eglot + company-capf for C/C++.
company-shell is retained for shell-mode completion.
#+BEGIN_SRC emacs-lisp
  (use-package company
    :ensure t
    :config
    (setq company-idle-delay 0)
    (setq company-minimum-prefix-length 3))

  (with-eval-after-load 'company
    (define-key company-active-map (kbd "M-n") nil)
    (define-key company-active-map (kbd "M-p") nil)
    (define-key company-active-map (kbd "C-n") #'company-select-next)
    (define-key company-active-map (kbd "C-p") #'company-select-previous)
    (define-key company-active-map (kbd "SPC") #'company-abort)
    (add-hook 'python-mode-hook 'company-mode)
    (add-hook 'c++-mode-hook   'company-mode)
    (add-hook 'c-mode-hook     'company-mode))

  (use-package company-shell
    :ensure t
    :config
    (require 'company)
    (add-hook 'shell-mode-hook 'shell-mode-company-init))

  (defun shell-mode-company-init ()
    (setq-local company-backends '((company-shell
                                    company-shell-env
                                    company-etags
                                    company-dabbrev-code))))
#+END_SRC

** line numbers and programming
#+BEGIN_SRC emacs-lisp
  ;; (add-hook 'after-init-hook (lambda () (global-display-line-numbers-mode t)))
  (add-hook 'prog-mode-hook 'display-line-numbers-mode)
#+END_SRC

* org mode
** keybindings
#+BEGIN_SRC emacs-lisp
  (global-set-key (kbd "C-c l") #'org-store-link)
  (global-set-key (kbd "C-c a") #'org-agenda)
  (global-set-key (kbd "C-c c") #'org-capture)
#+END_SRC

** agenda files
Only load files that actually exist on this machine.
#+BEGIN_SRC emacs-lisp
  (setq org-agenda-files
        (seq-filter #'file-exists-p
                    '("/home/fethi/snkr/org/ajanda.kisisel"
                      "/home/fethi/snkr/org/ajanda.bilimsel"
                      "/home/fethi/snkr/org/ajanda.mesleki"
                      "/home/fethi/snkr/org/gunce.kisisel"
                      "/home/fethi/snkr/org/gunce.bilimsel"
                      "/home/fethi/snkr/org/gunce.mesleki")))
#+END_SRC

** logging, drawer settings
customizations from Rainer König
#+BEGIN_SRC emacs-lisp
  (setq org-log-into-drawer t)
  (setq org-log-reschedule 'note)
  (setq org-refile-allow-creating-parent-nodes 'confirm)
  (setq org-refile-targets '((org-agenda-files :level . 1)))
  (setq org-refile-use-outline-path 'file)
#+END_SRC

** org-babel
Single consolidated babel languages block.
#+begin_src emacs-lisp
  (org-babel-do-load-languages
   'org-babel-load-languages
   '((maxima . t)
     (octave . t)
     (python . t)
     (shell  . t)))
#+end_src

* latex
#+BEGIN_SRC emacs-lisp
  (use-package tex
    :ensure auctex
    :defer t
    :hook (LaTeX-mode . LaTeX-math-mode)
    :init
    (setq TeX-auto-save t)
    (setq TeX-parse-self t)
    :config
    (setq TeX-PDF-mode t)
    (setq TeX-source-correlate-mode t)
    (setq TeX-source-correlate-start-server t)
    (setq TeX-view-program-selection '((output-pdf "PDF Tools"))
          TeX-view-program-list '(("PDF Tools" TeX-pdf-tools-sync-view)))
    (add-hook 'LaTeX-mode-hook 'turn-on-reftex)
    (with-eval-after-load 'latex
      (define-key LaTeX-mode-map (kbd "C-c C-c") 'TeX-command-master)
      (define-key LaTeX-mode-map (kbd "C-c C-v") 'TeX-view))
    (add-hook 'LaTeX-mode-hook 'visual-line-mode))

  (use-package auctex
    :ensure t
    :defer t)
#+END_SRC

** PDF Tools Configuration
#+BEGIN_SRC emacs-lisp
  (use-package pdf-tools
    :ensure t
    :config
    (pdf-tools-install)
    (add-hook 'pdf-view-mode-hook (lambda () (display-line-numbers-mode -1))))
#+END_SRC

** yasnippet stuff
#+BEGIN_SRC emacs-lisp
  (use-package yasnippet
    :ensure t
    :config
    (setq yas-snippet-dirs '("~/.emacs.d/snippets"))
    (yas-global-mode 1))
#+END_SRC

** reveal.js stuff
#+BEGIN_SRC elisp
  (use-package ox-reveal
    :ensure ox-reveal)

  (setq org-reveal-root "file:///home/fethi/software/reveal.js")
  ;;(setq org-reveal-root "https://cdn.jsdelivr.net/npm/reveal.js")
  (setq org-reveal-mathjax t)

  (use-package htmlize
    :ensure t)
#+END_SRC

* language modes
#+BEGIN_SRC emacs-lisp
  (add-hook 'org-mode-hook
            '(lambda ()
               (visual-line-mode 1)))

  (global-set-key (kbd "C-c '") 'org-edit-src-code)

  (add-hook 'emacs-lisp-mode-hook 'eldoc-mode)
  (add-hook 'emacs-lisp-mode-hook 'yas-minor-mode)
  (add-hook 'c++-mode-hook 'yas-minor-mode)
  (add-hook 'python-mode-hook 'yas-minor-mode)
  (add-hook 'c-mode-hook 'yas-minor-mode)
  (add-hook 'emacs-lisp-mode-hook 'company-mode)
  (setq org-ellipsis " ▾")
  (setq org-src-fontify-natively t)
  (setq org-src-tab-acts-natively t)
  (setq org-confirm-babel-evaluate nil)
  (setq org-export-with-smart-quotes t)
  (setq org-src-window-setup 'current-window)
  (add-hook 'org-mode-hook 'org-indent-mode)
#+END_SRC

** gud - grand unified debugger
gud is a built-in package; :ensure nil avoids a spurious MELPA lookup.
#+begin_src emacs-lisp
  (use-package gud
    :ensure nil
    :config
    (defun gud-gdb-massage-args (file args)
      "Massage arguments for GDB."
      args)

    (defun gud-gdb-marker-filter (string)
      "Filter GDB markers."
      string)

    (defun gud-gdb-find-file (file)
      "Find the source file for GDB."
      (find-file-noselect file))

    (defun gud-octave ()
      "Run Octave GUD."
      (interactive)
      (gud-common-init "octave" 'gud-gdb-massage-args 'gud-gdb-marker-filter 'gud-gdb-find-file)
      (set (make-local-variable 'gud-minor-mode) 'gud-octave)
      (gud-def gud-break  "dbstop in %f at %l"   "\C-b" "Set breakpoint at current line.")
      (gud-def gud-remove "dbclear at %l in %f"  "\C-d" "Remove breakpoint at current line.")
      (gud-def gud-step   "dbstep %p"            "\C-s" "Step one source line with display.")
      (gud-def gud-next   "dbnext %p"            "\C-n" "Step one line (skip functions).")
      (gud-def gud-cont   "dbcont"               "\C-r" "Continue with display.")
      (gud-def gud-finish "dbquit"               "\C-f" "Finish executing current function.")
      (gud-def gud-print  "dbprint %e"           "\C-p" "Print expression at point.")
      (setq comint-prompt-regexp "^(debug|more|octave|db> *\\|octave.*[\\]: *)")
      (setq paragraph-start comint-prompt-regexp)
      (run-hooks 'gud-octave-hook)))

  (add-hook 'gud-mode-hook
            (lambda ()
              (local-set-key (kbd "<f5>")    'gud-cont)
              (local-set-key (kbd "<f9>")    'gud-break)
              (local-set-key (kbd "<f10>")   'gud-next)
              (local-set-key (kbd "<f11>")   'gud-step)
              (local-set-key (kbd "S-<f11>") 'gud-finish)))
#+end_src

** flycheck
#+begin_src emacs-lisp
  (use-package flycheck
    :ensure t)

  (use-package flycheck-clang-analyzer
    :ensure t
    :config
    (with-eval-after-load 'flycheck
      (require 'flycheck-clang-analyzer)
      (flycheck-clang-analyzer-setup)))

  (add-hook 'LaTeX-mode-hook 'flyspell-mode)
  ;; (add-hook 'python-mode-hook 'flycheck-mode)  ; commented out: potential conflict with eglot
#+end_src

** git integration
#+BEGIN_SRC emacs-lisp
  (use-package magit
    :ensure t
    :config
    (setq magit-push-always-verify nil)
    (setq git-commit-summary-max-length 50)
    :bind
    ("M-g" . magit-status))
#+END_SRC

** python
Switching to the modern eglot + pyright.
*** eglot + pyright
#+BEGIN_SRC emacs-lisp
  (add-hook 'python-mode-hook 'eglot-ensure)
#+END_SRC

** C / C++
Using eglot for C/C++ LSP support (clangd backend), replacing the
former irony + company-irony + company-c-headers stack.
clangd must be installed on the system (clang package on Arch,
or via Homebrew on macOS: brew install llvm).
#+BEGIN_SRC emacs-lisp
  (add-hook 'c-mode-hook   'eglot-ensure)
  (add-hook 'c++-mode-hook 'eglot-ensure)

  ;; company-capf (built-in) feeds completions from eglot automatically;
  ;; set it as the primary backend for C/C++ buffers.
  (add-hook 'c-mode-hook
            (lambda ()
              (setq-local company-backends '(company-capf company-dabbrev-code))))
  (add-hook 'c++-mode-hook
            (lambda ()
              (setq-local company-backends '(company-capf company-dabbrev-code))))
#+END_SRC

** markdown
#+BEGIN_SRC emacs-lisp
  (use-package markdown-mode
    :ensure t
    :mode ("\\.md\\'" . markdown-mode)
    :init (setq markdown-command "pandoc"))
#+END_SRC

** maxima
Guard with executable-find so this block is silently skipped on
machines where Maxima is not installed (e.g. fethis-air).
#+BEGIN_SRC emacs-lisp
  (when (executable-find "maxima")
    (add-to-list 'load-path "/usr/share/emacs/site-lisp/maxima/")
    (use-package maxima
      :init
      (setq org-format-latex-options (plist-put org-format-latex-options :scale 2.0)
            maxima-display-maxima-buffer nil)
      (add-to-list 'auto-mode-alist  (cons "\\.mac\\'" 'maxima-mode))
      (add-to-list 'interpreter-mode-alist (cons "maxima" 'maxima-mode))))
#+END_SRC

#+begin_src maxima :results output
  programmode: false;
  eq: x^2 - 16 = 0;
  solution: solve(eq, x);
  print(solution);
#+end_src

#+RESULTS:
: solve: solution:
:                                     x = - 4
:                                      x = 4
: [%t1, %t2]

** octave
#+BEGIN_SRC emacs-lisp
  (use-package octave
    :ensure t
    :mode ("\\.m\\'" . octave-mode)
    :interpreter ("octave" . octave-mode)
    :config
    (setq octave-block-offset 4)
    (setq inferior-octave-startup-args '("--no-gui"))
    (add-hook 'inferior-octave-mode-hook
              (lambda ()
                (add-to-list 'comint-output-filter-functions 'ansi-color-process-output)
                (setq comint-prompt-read-only t)
                (setq comint-scroll-to-bottom-on-output t))))
#+END_SRC

#+BEGIN_SRC emacs-lisp
  (add-hook 'octave-mode-hook
            (lambda ()
              (electric-indent-local-mode -1)
              (auto-fill-mode -1)
              (company-mode -1)
              (abbrev-mode 1)
              (if (eq window-system 'x)
                  (font-lock-mode 1))
              (define-key octave-mode-map (kbd "C-c C-z") 'run-octave)
              (define-key octave-mode-map (kbd "C-c C-r") 'octave-send-region)
              (define-key octave-mode-map (kbd "C-c C-b") 'octave-send-buffer)
              (define-key octave-mode-map (kbd "C-c C-l") 'octave-send-line)
              (local-set-key (kbd "C-c C-d") 'gud-octave)
              (local-set-key (kbd "C-c b")   'gud-break)
              (local-set-key (kbd "C-c u")   'gud-remove)
              (local-set-key (kbd "C-c s")   'gud-step)
              (local-set-key (kbd "C-c n")   'gud-next)
              (local-set-key (kbd "C-c f")   'gud-finish)
              (local-set-key (kbd "C-c p")   'gud-print)))
#+END_SRC

* Kill ring
** Maximum entries on the ring
#+BEGIN_SRC emacs-lisp
  (setq kill-ring-max 100)
#+END_SRC

** popup-kill-ring
#+BEGIN_SRC emacs-lisp
  (use-package popup-kill-ring
    :ensure t
    :bind ("M-y" . popup-kill-ring)
    :config
    (with-eval-after-load 'popup
      (define-key popup-kill-ring-keymap (kbd "C-n") 'popup-next)
      (define-key popup-kill-ring-keymap (kbd "C-p") 'popup-previous)))
#+END_SRC

* convenient functions
** config edit
#+BEGIN_SRC emacs-lisp
  (defun config-visit ()
    (interactive)
    (find-file "~/.emacs.d/config.org"))
  (global-set-key (kbd "C-c e") 'config-visit)
#+END_SRC

** config reload
#+BEGIN_SRC emacs-lisp
  (defun config-reload ()
    (interactive)
    (org-babel-load-file (expand-file-name "~/.emacs.d/config.org")))
  (global-set-key (kbd "C-c r") 'config-reload)
#+END_SRC

** copy-whole-line
#+BEGIN_SRC emacs-lisp
  (defun copy-whole-line ()
    (interactive)
    (save-excursion
      (kill-new
       (buffer-substring
        (point-at-bol)
        (point-at-eol)))))
  (global-set-key (kbd "C-c w l") 'copy-whole-line)
#+END_SRC

** kill-whole-word function
#+BEGIN_SRC emacs-lisp
  (defun kill-whole-word ()
    (interactive)
    (backward-word)
    (kill-word 1))
  (global-set-key (kbd "C-c w w") 'kill-whole-word)
#+END_SRC
